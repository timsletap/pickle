# Backend API Configuration
# For local development, use your machine's local IP address
# Find your IP: Windows (ipconfig), Mac/Linux (ifconfig)
EXPO_PUBLIC_API_URL=http://192.168.1.137:8000

# Firebase Configuration (already configured in code)
# No changes needed here - using existing Firebase config

# Optional: YouTube API Key (for drill search feature)
# Get one at: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your_youtube_api_key_here

# Optional: Google Custom Search API (for equipment search)
# Get one at: https://console.cloud.google.com/apis/credentials
GOOGLE_SEARCH_API_KEY=your_google_search_api_key_here
GOOGLE_SEARCH_ENGINE_ID=your_search_engine_id_here
